[package]
name = "zoya-package"
version = "0.1.0"
edition = "2024"

[dependencies]
zoya-ast = { path = "../zoya-ast" }
zoya-naming = { path = "../zoya-naming" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
